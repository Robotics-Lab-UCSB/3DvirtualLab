export function processNode(idx: string, dependencies: string[], type: string): void {
    console.log(`Processing Node: ${idx}`);
    console.log(`Type: ${type}`);
    console.log(`Dependencies:`, dependencies);
  }
  